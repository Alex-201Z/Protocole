# Assets/UI

Place UI prefabs, sprites, and UI-specific scripts here.

Build the UI only after the core systems (Card, Deck, Combat, Run) are stable.
See `docs/PROJECT_CONTEXT.md` for the recommended dev order.
